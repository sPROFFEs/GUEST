# AUTOGENERATED — managed by panel applier.
# Static DHCP leases for known internal hosts.
{% for h in static_hosts %}
dhcp-host={{ h.mac }},{{ h.ip }}{% if h.hostname %},{{ h.hostname }}{% endif %}
{% endfor %}
